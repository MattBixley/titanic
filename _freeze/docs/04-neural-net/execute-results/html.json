{
  "hash": "06af9108238dc8fbfbbb71f60fc924d4",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Model 3 — Neural Net\"\nsubtitle: \"A small multilayer perceptron with nnet — and why scale matters\"\n---\n\n> **Where we are:** Last of three model documents in the\n> [Titanic — Tidymodels Tour](../index.qmd). Same structure as the\n> [XGBoost](02-xgboost.qmd) and [Random Forest](03-random-forest.qmd) docs —\n> recipe → spec → workflow → tune → finalize → evaluate → save. The one twist:\n> a neural net needs **normalized** inputs, so this doc extends the shared\n> `make_recipe()` with a normalization step.\n\n## Why a neural net here?\n\nOn a dataset this small (891 rows) a multilayer perceptron will not beat the\ntree ensembles — and that's fine. The point is to *explore* a different model\nfamily and see how tidymodels makes swapping engines almost free. A small MLP\nwith a single hidden layer and weight decay (`penalty`) is a clean, instructive\nchoice.\n\n::: {.callout-note}\n## Engine choice: `nnet`\nWe fit the MLP with base R's `nnet` engine. It has no native-code dependency, so\nit trains identically on your laptop, in the Quarto site build, and in GitHub\nActions CI. A `torch`-backed alternative (`brulee`) is shown later as an optional\nlocal upgrade — but it does **not** load inside this project's render/CI session,\nso `nnet` is the engine we publish.\n:::\n\n## Setup\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsource(here::here(\"R\", \"00_setup.R\"))      # tidyverse + tidymodels, seed = 42\nsource(here::here(\"R\", \"02_features.R\"))   # make_recipe(data)\n\nlibrary(nnet)     # the MLP engine (base R, no native deps)\nlibrary(vip)      # variable importance plots\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntrain <- readr::read_csv(here::here(\"data\", \"raw\", \"train.csv\")) |>\n  dplyr::mutate(Survived = factor(Survived, levels = c(\"0\", \"1\")))\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Tear down the PSOCK cluster from 00_setup.R and tune single-process. The grid\n# is tiny, so this is plenty fast and keeps results deterministic (no worker\n# package-loading surprises).\nif (exists(\"cl\")) try(parallel::stopCluster(cl), silent = TRUE)\nforeach::registerDoSEQ()\n```\n:::\n\n\n## The recipe — with normalization\n\nTree ensembles are invariant to monotone rescaling, so the shared recipe didn't\nbother normalizing. A neural net is **not** invariant: gradient descent behaves\nfar better when every numeric predictor has mean 0 and unit variance, otherwise\nlarge-scale features (like `Fare`) dominate the gradient. So we take the shared\nrecipe and append `step_normalize()`.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Extend the shared recipe with normalization of all numeric predictors. This\n# step must come AFTER dummy encoding in make_recipe() so the 0/1 dummy columns\n# are normalized too (standard practice for MLPs).\nnnet_recipe <- make_recipe(train) |>\n  step_normalize(all_numeric_predictors())\n\nnnet_recipe\n```\n\n::: {.cell-output .cell-output-stderr}\n\n```\n\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n── Recipe ──────────────────────────────────────────────────────────────────────\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n── Inputs \n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\nNumber of variables by role\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\noutcome:    1\npredictor: 10\nid:         1\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n── Operations \n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Variable mutation for: str_extract(Name, \"(?<=, )[A-Za-z]+(?=\\\\.)\"), ...\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Collapsing factor levels for: Title\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Variable mutation for: SibSp + Parch + 1L, ...\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Variable mutation for: str_sub(Cabin, 1, 1), ...\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Variables removed: Name, Ticket, Cabin\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• K-nearest neighbor imputation for: Age\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Mode imputation for: Embarked\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Median imputation for: Fare\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Variable mutation for: as.character(Pclass)\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Novel factor level assignment for: all_nominal_predictors()\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Dummy variables from: all_nominal_predictors()\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Zero variance filter on: all_predictors()\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stderr}\n\n```\n• Centering and scaling for: all_numeric_predictors()\n```\n\n\n:::\n:::\n\n\n## The model specification\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Single-hidden-layer MLP via base R's nnet engine: one hidden layer\n# (hidden_units) and L2 weight decay (penalty). nnet has no native-code\n# dependency, so it trains and renders reliably everywhere — including the\n# Quarto site build and GitHub Actions CI.\nnnet_spec <- mlp(\n  hidden_units = tune(),\n  penalty      = tune()\n) |>\n  set_engine(\"nnet\") |>\n  set_mode(\"classification\")\n\nnnet_spec\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nSingle Layer Neural Network Model Specification (classification)\n\nMain Arguments:\n  hidden_units = tune()\n  penalty = tune()\n\nComputational engine: nnet \n```\n\n\n:::\n:::\n\n\n::: {.callout-tip}\n## Optional: a torch-backed MLP with `brulee` (local only)\nFor a deeper net with a tuned epoch budget you can swap in `brulee` (a `torch`\nbackend) **when running locally**. It also exposes `epochs`:\n\n```r\nnnet_spec <- mlp(\n  hidden_units = tune(),\n  penalty      = tune(),\n  epochs       = tune()       # brulee only\n) |>\n  set_engine(\"brulee\") |>\n  set_mode(\"classification\")\n```\n\nWe keep `nnet` as the published engine on purpose: `brulee`'s libtorch backend\ndoes not load inside the Quarto/knitr render session (or in CI) under this\nproject's `renv` setup — every fit fails with *\"Lantern is not loaded\"*. `nnet`\nsidesteps that entirely. If you switch to `brulee`, add `epochs` back to the\ntuning grid below and train sequentially (`foreach::registerDoSEQ()`).\n:::\n\n## The workflow\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnnet_wf <- workflow() |>\n  add_recipe(nnet_recipe) |>\n  add_model(nnet_spec)\n```\n:::\n\n\n## Tuning with 5-fold cross-validation\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(42)\nfolds <- vfold_cv(train, v = 5, strata = Survived)\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Modest grid for a tiny dataset: a handful of hidden units and light-to-moderate\n# weight decay. (nnet has no epochs hyperparameter — it iterates to convergence.)\nset.seed(42)\nnnet_grid <- grid_space_filling(\n  hidden_units(range = c(2L, 16L)),\n  penalty(range = c(-4, -0.5)),     # log10 scale → 1e-4 to ~0.3\n  size = 12\n)\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnnet_metrics <- metric_set(roc_auc, accuracy)\n\nset.seed(42)\nnnet_tuned <- tune_grid(\n  nnet_wf,\n  resamples = folds,\n  grid      = nnet_grid,\n  metrics   = nnet_metrics,\n  # Positive class \"1\" is the second factor level.\n  control   = control_grid(save_pred = TRUE, event_level = \"second\")\n)\n```\n:::\n\n\n### Tuning results\n\n\n::: {.cell}\n\n```{.r .cell-code}\nautoplot(nnet_tuned, metric = \"roc_auc\")\n```\n\n::: {.cell-output-display}\n![ROC-AUC across the MLP tuning grid (autoplot).](04-neural-net_files/figure-html/tune-plot-1.png){width=864}\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nshow_best(nnet_tuned, metric = \"roc_auc\", n = 5)\n```\n\n::: {.cell-output-display}\n`````{=html}\n<div data-pagedtable=\"false\">\n  <script data-pagedtable-source type=\"application/json\">\n{\"columns\":[{\"label\":[\"hidden_units\"],\"name\":[1],\"type\":[\"int\"],\"align\":[\"right\"]},{\"label\":[\"penalty\"],\"name\":[2],\"type\":[\"dbl\"],\"align\":[\"right\"]},{\"label\":[\".metric\"],\"name\":[3],\"type\":[\"chr\"],\"align\":[\"left\"]},{\"label\":[\".estimator\"],\"name\":[4],\"type\":[\"chr\"],\"align\":[\"left\"]},{\"label\":[\"mean\"],\"name\":[5],\"type\":[\"dbl\"],\"align\":[\"right\"]},{\"label\":[\"n\"],\"name\":[6],\"type\":[\"int\"],\"align\":[\"right\"]},{\"label\":[\"std_err\"],\"name\":[7],\"type\":[\"dbl\"],\"align\":[\"right\"]},{\"label\":[\".config\"],\"name\":[8],\"type\":[\"chr\"],\"align\":[\"left\"]}],\"data\":[{\"1\":\"2\",\"2\":\"0.0168761248\",\"3\":\"roc_auc\",\"4\":\"binary\",\"5\":\"0.8533541\",\"6\":\"5\",\"7\":\"0.007546032\",\"8\":\"pre0_mod01_post0\"},{\"1\":\"4\",\"2\":\"0.1519911083\",\"3\":\"roc_auc\",\"4\":\"binary\",\"5\":\"0.8522011\",\"6\":\"5\",\"7\":\"0.006679865\",\"8\":\"pre0_mod03_post0\"},{\"1\":\"10\",\"2\":\"0.3162277660\",\"3\":\"roc_auc\",\"4\":\"binary\",\"5\":\"0.8521187\",\"6\":\"5\",\"7\":\"0.009145477\",\"8\":\"pre0_mod08_post0\"},{\"1\":\"5\",\"2\":\"0.0038986037\",\"3\":\"roc_auc\",\"4\":\"binary\",\"5\":\"0.8481136\",\"6\":\"5\",\"7\":\"0.004586083\",\"8\":\"pre0_mod04_post0\"},{\"1\":\"3\",\"2\":\"0.0004328761\",\"3\":\"roc_auc\",\"4\":\"binary\",\"5\":\"0.8411387\",\"6\":\"5\",\"7\":\"0.006383683\",\"8\":\"pre0_mod02_post0\"}],\"options\":{\"columns\":{\"min\":{},\"max\":[10]},\"rows\":{\"min\":[10],\"max\":[10]},\"pages\":{}}}\n  </script>\n</div>\n`````\n:::\n:::\n\n\n## Finalize and fit on the full training set\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbest_nnet <- select_best(nnet_tuned, metric = \"roc_auc\")\nbest_nnet\n```\n\n::: {.cell-output-display}\n`````{=html}\n<div data-pagedtable=\"false\">\n  <script data-pagedtable-source type=\"application/json\">\n{\"columns\":[{\"label\":[\"hidden_units\"],\"name\":[1],\"type\":[\"int\"],\"align\":[\"right\"]},{\"label\":[\"penalty\"],\"name\":[2],\"type\":[\"dbl\"],\"align\":[\"right\"]},{\"label\":[\".config\"],\"name\":[3],\"type\":[\"chr\"],\"align\":[\"left\"]}],\"data\":[{\"1\":\"2\",\"2\":\"0.01687612\",\"3\":\"pre0_mod01_post0\"}],\"options\":{\"columns\":{\"min\":{},\"max\":[10]},\"rows\":{\"min\":[10],\"max\":[10]},\"pages\":{}}}\n  </script>\n</div>\n`````\n:::\n\n```{.r .cell-code}\nfinal_nnet_wf <- finalize_workflow(nnet_wf, best_nnet)\n\nset.seed(42)\nnnet_fit <- fit(final_nnet_wf, data = train)\n```\n:::\n\n\n## Cross-validated performance\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# nnet tunes hidden_units + penalty (no epochs). If you switch to the brulee\n# engine, add \"epochs\" to the join keys.\ncollect_metrics(nnet_tuned) |>\n  dplyr::semi_join(best_nnet,\n                   by = c(\"hidden_units\", \"penalty\")) |>\n  dplyr::select(.metric, mean, std_err, n)\n```\n\n::: {.cell-output-display}\n`````{=html}\n<div data-pagedtable=\"false\">\n  <script data-pagedtable-source type=\"application/json\">\n{\"columns\":[{\"label\":[\".metric\"],\"name\":[1],\"type\":[\"chr\"],\"align\":[\"left\"]},{\"label\":[\"mean\"],\"name\":[2],\"type\":[\"dbl\"],\"align\":[\"right\"]},{\"label\":[\"std_err\"],\"name\":[3],\"type\":[\"dbl\"],\"align\":[\"right\"]},{\"label\":[\"n\"],\"name\":[4],\"type\":[\"int\"],\"align\":[\"right\"]}],\"data\":[{\"1\":\"accuracy\",\"2\":\"0.8238067\",\"3\":\"0.006656481\",\"4\":\"5\"},{\"1\":\"roc_auc\",\"2\":\"0.8533541\",\"3\":\"0.007546032\",\"4\":\"5\"}],\"options\":{\"columns\":{\"min\":{},\"max\":[10]},\"rows\":{\"min\":[10],\"max\":[10]},\"pages\":{}}}\n  </script>\n</div>\n`````\n:::\n:::\n\n\n::: {.callout-tip}\nCopy the `accuracy` and `roc_auc` `mean` values into the results table on the\n[master document](../index.qmd).\n:::\n\n### Confusion matrix\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnnet_cv_preds <- collect_predictions(nnet_tuned, parameters = best_nnet)\n\nnnet_cv_preds |>\n  conf_mat(truth = Survived, estimate = .pred_class) |>\n  autoplot(type = \"heatmap\") +\n  ggplot2::labs(title = \"Neural Net — CV confusion matrix\")\n```\n\n::: {.cell-output-display}\n![Confusion matrix from out-of-fold CV predictions.](04-neural-net_files/figure-html/confusion-1.png){width=480}\n:::\n:::\n\n\n### Variable importance\n\nNeural nets don't expose importance natively. For an `nnet` model `vip` derives\nit from the trained connection weights (Garson's algorithm) — no shuffling or\nprediction wrapper needed, so it's robust in any render environment.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Pull the underlying nnet engine object out of the fitted workflow and let vip\n# read importance straight from the network weights.\nnnet_fit |>\n  extract_fit_engine() |>\n  vip::vip(num_features = 15) +\n  ggplot2::labs(title = \"Neural Net variable importance (Garson weights)\") +\n  ggplot2::theme_minimal()\n```\n\n::: {.cell-output-display}\n![Connection-weight (Garson) importance for the MLP.](04-neural-net_files/figure-html/vip-1.png){width=672}\n:::\n:::\n\n\nEven through a different lens, the same features carry the weight — `Title`,\n`Sex`, class, `Fare` — consistent with [XGBoost](02-xgboost.qmd) and the\n[Random Forest](03-random-forest.qmd).\n\n## Save the fitted workflow\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Project convention: the neural-net model saves to nnet_fit.rds.\nsaveRDS(nnet_fit, file = here::here(\"models\", \"nnet_fit.rds\"))\n```\n:::\n\n\n## Submission\n\nGenerate this model's leaderboard CSV with the\n[parameterized submission document](submission.qmd):\n\n```bash\nquarto render docs/submission.qmd -P model:nnet\n```\n\nThat loads `models/nnet_fit.rds`, predicts on `data/raw/test.csv`, writes\n`models/submission_nnet.csv`, and prints the exact `kaggle competitions submit`\ncommand. See the [Kaggle setup guide](setup-kaggle.md) for authentication.\n\n## Next steps\n\n- You've now fit all three families — compare their CV scores side by side in\n  the **[master results table](../index.qmd)**.\n- Revisit **[XGBoost](02-xgboost.qmd)** and the **[Random Forest](03-random-forest.qmd)**.\n- Build the CSV with the **[Submission doc](submission.qmd)** and push it to the\n  leaderboard.\n",
    "supporting": [
      "04-neural-net_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {
      "include-in-header": [
        "<link href=\"../site_libs/pagedtable-1.1/css/pagedtable.css\" rel=\"stylesheet\" />\n<script src=\"../site_libs/pagedtable-1.1/js/pagedtable.js\"></script>\n"
      ]
    },
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}